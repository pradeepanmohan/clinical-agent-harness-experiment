# Harness scripts

Future S06 hardening should add executable checks here:

- `check-allowed-files.ts`
- `check-evidence.ts`
- `check-task-status.ts`

For the initial baseline these are documented as planned harness checks rather than implemented scripts.
